<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.*?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane prefHeight="500.0" prefWidth="950.0"
            xmlns="http://javafx.com/javafx/17.0.12"
            xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="gui.cover.CoverDomainController">

    <top>
        <VBox spacing="10">
            <padding>
                <Insets bottom="10" left="10" right="10" top="10"/>
            </padding>

            <!-- 🔹 Titre principal -->
            <Label fx:id="domainTitle"
                   style="-fx-font-size: 20px; -fx-font-weight: bold;"/>

            <!-- 🔹 Sélecteur de Kernfrage -->
            <HBox alignment="CENTER_LEFT" spacing="10">
                <Label text="Kernfrage:"/>
                <ChoiceBox fx:id="kernfrageChoice" prefWidth="400"/>
            </HBox>

            <!-- 🔹 Paramètres dynamiques -->
            <HBox fx:id="parameter" spacing="20" visible="true" managed="true">

                <!-- 🔸 Vertragsstand -->
                <VBox spacing="6">
                    <Label text="Vertragsstand"/>
                    <!-- remplacé : ComboBox → ListView -->
                    <ListView fx:id="vertragsstandList" prefHeight="100" prefWidth="200"/>
                    <Label fx:id="vertragsstandSelectedLabel"
                           text="Ausgewählt: -"
                           wrapText="true"
                           maxWidth="200"
                           style="-fx-font-style: italic; -fx-text-fill: gray;"/>
                </VBox>

                <!-- 🔸 Bearbeitungsstand -->
                <VBox spacing="6">
                    <Label text="Bearbeitungsstand"/>
                    <ListView fx:id="bearbeitungsstandList" prefHeight="100" prefWidth="200"/>
                    <Label fx:id="bastandSelectedLabel"
                           text="Ausgewählt: -"
                           wrapText="true"
                           maxWidth="200"
                           style="-fx-font-style: italic; -fx-text-fill: gray;"/>
                </VBox>

                <!-- 🔸 Gruppierung -->
                <VBox spacing="6">
                    <HBox spacing="10" alignment="CENTER_LEFT">
                        <Label text="Gruppierung"/>
                        <CheckBox fx:id="groupByAllCheck" text="Alles"/>
                    </HBox>
                    <ListView fx:id="groupByList" prefHeight="100" prefWidth="200"/>
                    <Label fx:id="groupBySelectedLabel"
                           text="Ausgewählt: Standard (all)"
                           wrapText="true"
                           maxWidth="200"
                           style="-fx-font-style: italic; -fx-text-fill: gray;"/>
                </VBox>

                <!-- 🔸 Date filters -->
                <HBox fx:id="dateBox" spacing="20" visible="false" managed="false">
                    <VBox spacing="6">
                        <Label text="Ab-Datum"/>
                        <DatePicker fx:id="abDatePicker"/>
                    </VBox>
                    <VBox spacing="6">
                        <Label text="Bis-Datum"/>
                        <DatePicker fx:id="bisDatePicker"/>
                    </VBox>
                </HBox>

                <!-- 🔸 Stornogründe -->
                <!-- ✅ visible uniquement pour la KF “Abgelehnte/Storno Angebote” -->
                <VBox fx:id="stornoGrundFilterBox" spacing="6" visible="false" managed="false">
                    <Label text="Stornogründe filtern"/>
                    <ListView fx:id="stornoGrundList" prefHeight="100" prefWidth="220"/>
                </VBox>

                <!-- 🔸 Exécution -->
                <VBox alignment="BOTTOM_RIGHT">
                    <Button fx:id="ausfuehrenButton"
                            onAction="#runKernfrage"
                            text="🔍 Ausführen"
                            disable="true"/>
                </VBox>

            </HBox>
        </VBox>
    </top>

    <!-- 🔹 Table & Messages -->
    <center>
        <VBox spacing="10" VBox.vgrow="ALWAYS">
            <Label fx:id="messageLabel" style="-fx-text-fill: gray;"/>
            <VBox fx:id="resultsContainer" VBox.vgrow="ALWAYS"/>
        </VBox>
    </center>

    <!-- 🔹 Bouton Fermer -->
    <bottom>
        <HBox alignment="CENTER_RIGHT">
            <Button onAction="#closeWindow" text="Schließen"/>
        </HBox>
    </bottom>

</BorderPane>
